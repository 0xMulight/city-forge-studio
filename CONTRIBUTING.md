# Contributing

欢迎改进 City Forge Studio。

## 开发步骤

```powershell
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

## 提交前检查

```powershell
npm run build
```

请确认不要提交：

- `.env` 或 `.env.*`
- 密钥文件
- `node_modules/`
- `dist/`
- 本地日志文件

## 建议方向

- 更真实的 3D 建筑模型
- 更完整的天气系统
- 模块拖拽和单层移动
- 数据导入 / 导出
- 移动端布局优化
